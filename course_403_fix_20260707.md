# 课程403修复与主页下拉菜单 — 2026-07-07

## 目标
1. 主页图书馆和在线课程链接移到"更多"下拉菜单
2. 修复课程点进去没有内容显示的 bug

## 根因分析
课程详情页点课时无内容的根因：**SecurityConfig 中 `requestMatchers` 的 HTTP 方法参数写法错误**

错误写法：
```kotlin
.requestMatchers("/api/v1/courses/*/lessons/*", "DELETE").hasAnyRole("ADMIN", "TEACHER")
```
Spring Security 将第二个字符串参数 `"DELETE"` 当作 URL pattern 而非 HTTP 方法，导致**所有方法**（包括 GET）的 `/courses/*/lessons/*` 请求都要求 ADMIN/TEACHER 角色，未登录用户收到 403。

正确写法：
```kotlin
.requestMatchers(HttpMethod.DELETE, "/api/v1/courses/*/lessons/*").hasAnyRole("ADMIN", "TEACHER")
```

## 修复内容

### 1. SecurityConfig 修复（核心）
- 三行 `requestMatchers` 从字符串方法改为 `HttpMethod.DELETE` 枚举
- 影响：`/courses/*/sections/*`、`/courses/*/lessons/*`、`/courses/*/exams/*` 的 GET 请求不再被拦截

### 2. 主页 topbar 下拉菜单
- 从 topbar 移除图书馆和在线课程的独立链接
- 在"更多"下拉菜单顶部添加图书馆和在线课程链接（带分隔线）
- 版本号 v=20260707b

## 验证结果
- Lesson API: `/courses/8/lessons/36` 返回 200 + 227字符内容（之前 403）
- 课程列表: 4门课程全部正常
- 前端页面: /、/courses.html、/library.html、/settings.html、/login.html 全部 200
- topbar 中图书馆/在线课程: 已移到下拉菜单

## 当前服务状态
- 后端: PID 运行中，端口 8080，-Xmx768m
- 前端: 端口 3000
- Git: commit 4dbd3aa (monolith-clean)
- 课程数据: 4门课程（大使命/新生命/基要真理/祷告生活），共 8 单元 17+8+9+6=40 课
