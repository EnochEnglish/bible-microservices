# 前端恢复 — 2026-06-07 08:47

## 背景
用户反馈"边栏改错了，中英对照没有了，经文章节点击没有了"——上一轮 LLM 将前端从 git mirror 版本（6月2日）重构为 BibleAPI 类后引入了多个 bug。

## 关键发现
当前工作区前端与 git mirror（D:\dev\github\bible-microservices\frontend\）架构完全不同：
- **Git mirror (6月2日)**：使用 `apiGet()` + 硬编码 `OT_BOOKS`/`NT_BOOKS`/`BOOK_ORDER` 数组（含中英文名+章节数），`renderBookList()` 用 `merge()` 将硬编码数据与 API 数据合并
- **当前/重构版**：使用 `BibleAPI` 类调用 API，`renderSidebar()` 直接从 API 数据渲染，丢失了硬编码的中文名和 click handler

## 操作
1. 从 git mirror 恢复 `app.js`（48KB）、`api.js`（1.5KB）、`config.js`（2KB）
2. 保留 `server.js` 全 API 代理（消除 CORS 问题）
3. 修改 `config.js`：所有模式返回 `/api/v1`（相对路径走代理）
4. **最小修复**：在 `app.js` 的 `loadBooks()` 中添加字段映射 `b.id = b.book_id; b.chapters = b.chapter_count`
   - API 返回 `book_id`/`chapter_count`，但代码用 `b.id`/`b.chapters`

## 验证
- JS 语法检查 3/3 ✅
- 代理端点 4/4 ✅
- HTML 元素完整性 ✅
- 中文名（创世记等）正常 ✅
