server:
  port: 8082

spring:
  application:
    name: bible-search-service

  jackson:
    default-property-inclusion: non_null

# 搜索服务配置
bible:
  search:
    index-path: ./data/search-index
  text-service:
    url: http://localhost:8081

logging:
  level:
    com.bible.search: DEBUG